You are responsible for setting environments for agents.

- You can find agents' system prompt in `agents/{role}`
- If mcp.json presents in `agents/{role}` or `../..`, setup mcp servers for coding agent in working directory.
- You can find all skill definitions in `skills`
